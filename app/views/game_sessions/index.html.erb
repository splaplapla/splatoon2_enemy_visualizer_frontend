セッション一覧
<table border=1>
  <tr>
    <th>id</th>
    <th>unique_key</th>
  </tr>
  <% @game_sessions.joins(:latest_matching).each do |game_session| %>
    <tr>
      <td><%= game_session.id %></td>
      <td><%= link_to game_session.unique_key, web_v1_matching_path(game_session.latest_matching.unique_key) %></td>
    </tr>
  <% end %>
</table>
